pool:
  name: Hosted VS2017
  demands:
  - msbuild
  - visualstudio
steps:
- task: qetza.replacetokens.replacetokens-task.replacetokens@3
  displayName: 'Replace tokens in **/*.config'
# Still same error. 
#This is for the testing PR approvers still its not working i made few changes to check still its throughing error this is used to test the code again.
- task: DotNetCoreCLI@2
  displayName: 'dotnet restore'
  inputs:
    command: restore
    projects: '**/*.csproj'
  enabled: false
  
- task: VSBuild@1
  displayName: 'Build solution **\*.sln'
  enabled: false